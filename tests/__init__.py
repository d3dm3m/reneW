# This file makes the 'tests' directory a Python package,
# which is necessary for relative imports to work correctly.
# For example, it allows test_dialog_logic.py to import from mock_utils.py
# using `from .mock_utils import ...`.
